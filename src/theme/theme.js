import { createTheme } from '@material-ui/core/styles';
import { palette, typography, overrides } from './';

const theme = createTheme({
  palette,
  typography,
  overrides,
});
export default theme;
